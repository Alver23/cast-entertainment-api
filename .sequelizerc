// Dependencies
const path = require('path');

const rootDir = 'build/database/';
const basePath = path.join(__dirname, rootDir);

module.exports = {
  'config': path.resolve('config', `${basePath}config/config.js`),
  'models-path': path.resolve('db', `${basePath}models`),
  'seeders-path': path.resolve('db', `${basePath}seeders`),
  'migrations-path': path.resolve('db', `${basePath}migrations`)
};
